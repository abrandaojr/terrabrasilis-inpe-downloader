# PRODES Pipeline — Python dependencies
# Install with:  uv pip install -r requirements.txt
#            or: pip install -r requirements.txt
#
# For geospatial packages on Windows, uv is strongly recommended:
#   pip install uv
#   uv pip install -r requirements.txt

# --- script 01 ---
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
tqdm>=4.66

# --- script 02 ---
geopandas>=1.0
pyogrio>=0.9
pyarrow>=17.0
shapely>=2.0
numpy>=1.26
rasterio>=1.3

# --- script 03 ---
matplotlib>=3.9

# --- optional: Selenium fallback for script 01 ---
# selenium>=4.20
# webdriver-manager>=4.0
